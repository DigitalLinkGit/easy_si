{% extends 'base.html.twig' %}

{% block title %}FormCapture{% endblock %}

{% block body %}
    <h1>FormCapture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_capture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ form_capture.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ form_capture.description }}</td>
            </tr>
            <tr>
                <th>RenderTemplate</th>
                <td>{{ form_capture.renderTemplate }}</td>
            </tr>
            <tr>
                <th>RenderTitle</th>
                <td>{{ form_capture.renderTitle }}</td>
            </tr>
            <tr>
                <th>RenderTitleLevel</th>
                <td>{{ form_capture.renderTitleLevel }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ form_capture.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_form_capture_index') }}">back to list</a>

    <a href="{{ path('app_form_capture_edit', {'id': form_capture.id}) }}">edit</a>

    {{ include('capture/compose/form_capture/_delete_form.html.twig') }}
{% endblock %}
