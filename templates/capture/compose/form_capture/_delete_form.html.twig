<form method="post" action="{{ path('app_form_capture_delete', {'id': form_capture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form_capture.id) }}">
    <button class="btn">Delete</button>
</form>
