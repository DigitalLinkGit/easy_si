{% extends 'base.html.twig' %}
{% block title %}Prévisualiser le rendu{% endblock %}

{% block body %}
  <div class="container mt-4">
    <h2>Question :</h2>
    <div>{{ instance.question.content }}</div>
    <div>Type : {{ instance.question.type.value }}</div>
    <div>Utilisez la variable <code>[{{ instance.question.name }}] </code>dans le texte.
    {{ form_start(form) }}
      {{ form_row(form.renderTemplate) }}

      <div class="mt-3">
        <a href="{{ path('app_quiz_edit', { id: quiz.id }) }}" class="btn btn-outline-secondary">Retour</a>
        <button class="btn btn-primary" type="submit">Enregistrer</button>
      </div>
    {{ form_end(form) }}
  </div>
{% endblock %}
