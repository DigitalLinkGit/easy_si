{% extends 'base.html.twig' %}

{% block title %}Capture{% endblock %}

{% block body %}
    <h1>Capture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ capture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ capture.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ capture.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_capture_index') }}">back to list</a>

    <a href="{{ path('app_capture_edit', {'id': capture.id}) }}">edit</a>

    {{ include('capture/_delete_form.html.twig') }}
{% endblock %}
