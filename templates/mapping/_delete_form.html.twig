<form method="post" action="{{ path('app_mapping_delete', {'id': mapping.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mapping.id) }}">
    <button class="btn">Delete</button>
</form>
