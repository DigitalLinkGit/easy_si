{% extends 'base.html.twig' %}

{% block title %}Interaction{% endblock %}

{% block body %}
    <h1>Interaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interaction.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ interaction.label }}</td>
            </tr>
            <tr>
                <th>ActionName</th>
                <td>{{ interaction.actionName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ interaction.description }}</td>
            </tr>
            <tr>
                <th>Logic</th>
                <td>{{ interaction.logic ? interaction.logic|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_interaction_index') }}">back to list</a>

    <a href="{{ path('app_interaction_edit', {'id': interaction.id}) }}">edit</a>

    {{ include('interaction/_delete_form.html.twig') }}
{% endblock %}
