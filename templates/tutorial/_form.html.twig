{{ form_start(form) }}
    
    {{ form_widget(form) }}
    {{ include('tutorial_Step/index.html.twig') }}
    <div class="d-flex justify-content-end gap-2 mt-4">
	<a href="{{ path('app_tutorial_index') }}" class="btn btn-outline-primary">Retour</a>
	<button type="submit" class="btn btn-primary">Enregistrer</button>
</div>
{{ form_end(form) }}
