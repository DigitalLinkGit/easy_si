{# fixe #}
<nav class="d-flex align-items-center gap-2 px-3 py-2 bg-white border-bottom shadow-sm"
     style="height: 60px; position: fixed; top: 0; width: 100%; z-index: 1050;">
  <button class="btn btn-outline-primary btn-sm btn-icon" id="toggleSidebar">☰</button>
  {#<img src="{{ asset('images/logo.svg') }}" alt="Logo" style="height: 30px;">#}
  <span class="fw-bold ms-2 app-title">FluencyS</span>
</nav>

{# Sidebar #}
<div id="sidebarPanel" class="position-fixed top-0 start-0 bg-white border-end shadow"
     style="width: 250px; height: 100vh; transform: translateX(-100%); transition: transform 0.3s; z-index: 1040; padding-top: 60px;">
  <div class="p-2">
    {# Section: Capture #}
    {% include 'global/partials/_sidebar_section.html.twig' with {
      icon: '',
      title: 'Capture',
      groups: [
        {
          links: [
			      { label: 'Mes Captures', route: 'app_capture_index' },
            { label: 'Mes Formulaires', route: 'app_form_capture_index' },
            { label: 'Mes Questionnaires', route: 'app_quiz_index' },
            { label: 'Mes Catégories', route: 'app_category_index' }
          ]
        }
      ]
    } %}

    {# Section: Design #}
    {% include 'global/partials/_sidebar_section.html.twig' with {
      icon: '',
      title: 'Design',
      groups: [
        {
          links: [
            { label: 'Éléments', route: 'app_element_index' },
            { label: 'Interactions', route: 'app_interaction_index' },
            { label: 'Flows', route: 'app_flow_index' },
			      { label: 'DataTables', route: 'app_datatable_index' }
          ]
        }
      ]
    } %}
    {# Section: Administration #}
    {% include 'global/partials/_sidebar_section.html.twig' with {
      icon: '',
      title: 'Administration',
      groups: [
        {
          links: [
            { label: 'Rôles', route: 'app_role_index' }
          ]
        }
      ]
    } %}
  </div>
</div>
